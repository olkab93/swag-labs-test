{
  "name": "swag-labs-test",
  "version": "1.0.0",
  "description": "Swag Labs test repo",
  "main": "index.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:open:problem": "cypress open --env user_name=problem_user",
    "cy:open:prefglitch": "cypress open --env user_name=performance_glitch_user",
    "cy:run": "cypress run -b chrome"
  },
  "keywords": [],
  "author": "olka bochenska",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/cypress": "^10.0.1",
    "cypress": "^13.2.0",
    "typescript": "^5.2.2"
  }
}
